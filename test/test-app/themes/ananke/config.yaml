module:
  hugoVersion:
    min: "0.64.0"
caches:
  assets:
    dir: :cacheDir/_gen
    maxAge: -1
  getcsv:
    dir: :cacheDir/:project
    maxAge: -1
  getjson:
    dir: :cacheDir/:project
    maxAge: -1
  images:
    dir: :cacheDir/_gen
    maxAge: -1
  modules:
    dir: :cacheDir/modules
    maxAge: -1